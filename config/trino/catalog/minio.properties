# Direct MinIO/S3 access without Hive Metastore
connector.name=hive
hive.metastore=file
hive.metastore.catalog.dir=s3a://zeek-data/

# S3 Configuration for MinIO
hive.s3.endpoint=http://minio:9000
hive.s3.aws-access-key=minioadmin
hive.s3.aws-secret-key=minioadmin
hive.s3.path-style-access=true
hive.s3.ssl.enabled=false

# Enable Parquet optimizations
hive.parquet.use-column-names=true
hive.parquet.fail-on-corrupted-statistics=false

# Performance tuning
hive.max-partitions-per-scan=100000
hive.max-partitions-for-eager-load=1000

# Compression
hive.compression-codec=SNAPPY

# Allow recursive directory listings
hive.recursive-directories=true

# File formats
hive.allow-drop-table=false
hive.allow-rename-table=false